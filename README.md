# time-wizard
TimeWizard is a collection of Linux hooking backends that spoof a process’s notion of time and file timestamps (via time clocks and filesystem timestamps) using LD_PRELOAD, ptrace, seccomp, and DBI—useful for testing, reproducibility, and analysis.
